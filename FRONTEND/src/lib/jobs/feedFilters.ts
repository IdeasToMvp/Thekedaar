/** @deprecated Import from `@/lib/launch` — kept for existing imports. */
export {
  filterJobsBySalary,
  pickFeaturedJob,
  roleIcon as categoryIcon,
  launchHighlights as categoryHighlights,
  type SalaryBandId as SalaryBand,
} from "@/lib/launch";
